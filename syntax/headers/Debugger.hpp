#ifndef DEBUGGER_HPP
#define DEBUGGER_HPP

#include <iostream>
#include <string>
#include "lexic.hpp"
#include "LexicalAnalyser.hpp"
#include "SyntaxAnalyser.hpp"


class Debugger {
public:
    /*static struct state estado;
    
    static void print_program(struct syntax::state& s);
    static void print_exp(struct syntax::state& s);
    static void print_eb(struct syntax::state& s);
    static void print_var(struct syntax::state& s);
    static void print_num(struct syntax::state& s);*/
};

#endif // DEBUGGER_HPP
